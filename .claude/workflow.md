# Workflow

- When in plan mode, always add a section on unit tests. Are new tests needed? Do existing tests need updated?

- After making changes to Python files, run `uv run ruff check --fix .` and `uv run ruff format .`.
